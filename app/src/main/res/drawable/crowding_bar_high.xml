<?xml version="1.0" encoding="utf-8"?>
<!-- Drawable resource for crowding bar high. Centralizes shape/color styling so UI theme updates are easier to maintain. -->

<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item>
        <shape android:shape="rectangle">
            <solid android:color="?attr/ajp_muted" />
            <corners android:radius="4dp" />
        </shape>
    </item>
    <item>
        <scale android:scaleWidth="75%">
            <shape android:shape="rectangle">
                <solid android:color="@color/destructive" />
                <corners android:radius="4dp" />
            </shape>
        </scale>
    </item>
</layer-list>

